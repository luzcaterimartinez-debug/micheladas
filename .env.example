# Michelandia POS — variables de entorno (frontend)
# Copia a .env.local o configura en tu hosting (Vercel, etc.)

# URL del API en producción (sin barra final)
# En desarrollo local puedes dejarlo vacío: Vite redirige /api → localhost:8000
# Ejemplo prod:  https://tu-app.vercel.app  (mismo dominio) o https://api.tu-dominio.com
VITE_API_URL=
