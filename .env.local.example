# Michelandia POS — desarrollo local
# Copia a .env.local (Vite lo carga automáticamente)

# Dejar vacío usa proxy de Vite → http://localhost:8000
# O descomenta para llamar al API directo:
# VITE_API_URL=http://localhost:8000
